# Readme

## Installation
We use the python venv module to create a virtual environment and pip to install the required packages.

Create environemnt with 
```bash
python3 -m venv .venv
```

Activate environment with 
```bash
source .venv/bin/activate
```

Install the required packages with 
```bash
pip install -r requirements.txt
```